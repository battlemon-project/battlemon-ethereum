[app]
host = "127.0.0.1"
port = 8000

[db]
host = "localhost"
port = 5432
username = "postgres"
password = "password"
db_name = "app_db"

[secrets]
#  private key generated by Ed25519 and represented in pkcs8v2 format and encoded by base64 standart encoding with pads.
key_pair = "this is secret"